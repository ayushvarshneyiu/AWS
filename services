to track changes and aws monitor iam activities?
    -->aws cloudtrail records api activity in your account including changes to iam users,policies and roles


* depending upon you cpu,memory, storage, networking  we choose type of instance required for an application.aws provide various instance families optimized for differnt use cases such as compute
memory,storage optimized and gpu instance.


ebs snapshot- its  a point in time copy of an EBS volume.
        uses==
                it is used to create backup.
                migrate data between region
                And to create volumes.

highly availbale - deploy the application in multi azs within a region and we can have instances behind elastic load balancer so as to distribute traffic. 
                
Q. isntance store and EBS volume
    instance store is local,temporary storage that is physically attached to host machine data persist as long as ec2 running.
    EBS volume is a network attached storage that provide durable and resizable block level storage.

user data property when launching ec2 instance-it is for bootup script,instance configiration,software installation,appplication setup.

yes we can share ami with other aws accounts too by making it public.


-*-- to secure access we implement iam policies and s3 policies to control access to your s3 bucket.additinally we can enable s3 access pointsto manange
access more granularly.

AWS secret manager is a service designed to secure storage and management of secrets including automatic rotation. 

===* dr setup for on premises we have aws data backup and aws storage gw

===aws organisation is used to centrally managed multiple aws account and then setup cross account iam roles to grant access to users fro mone account resources to anoteher account.

single signon is a service that allows you to manage mulitple acess to multiple aws accounts and applications. 

restrict access to certain ec2 instance based on specific tags env=prod so to achieve this we use iam policies with condition keys to control access based on ec2 instance tags.

=======================================================

ALB VS NLB
ALB operate at application layer it support content based routing and path based moslty handles tcp/udp request.

NLB operate at transport layer it provide low network policy and handles tcp/udp request.
